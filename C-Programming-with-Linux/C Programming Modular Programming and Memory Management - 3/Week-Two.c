//

/*
#include <stdio.h>
#include <stdbool.h>

int main(void){
    int count, main_sum_bytes = 0, num;
    char type;
    bool state = false;
    scanf("%d", &count);
    for(int i = 0; i<count;i++){
        scanf("%d %c", &num, &type);
        if(type=='i')
            main_sum_bytes+=num*4;
        else if(type=='c')
            main_sum_bytes+=num;
        else if(type=='d')
            main_sum_bytes+=num*8;
        else{
            state = true;

            break;
        }


    }
    if(state)
        printf("invalid tracking code type");
    else
        printf("%d bytes", main_sum_bytes);
    return 0;

}

*/

//

/*


*/